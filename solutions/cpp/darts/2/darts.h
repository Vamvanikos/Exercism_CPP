#pragma once

namespace darts {

inline constexpr float INNER_RADIUS_SQ = 1.0f * 1.0f;
inline constexpr float MIDDLE_RADIUS_SQ = 5.0f * 5.0f;
inline constexpr float OUTER_RADIUS_SQ = 10.0f * 10.0f;
    
int score(float x, float y);

}  // namespace darts
